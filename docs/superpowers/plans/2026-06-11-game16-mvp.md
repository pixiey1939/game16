# 解谜游戏16：消失的麻姐 — MVP 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 从 `script/game_design.txt`（v7.0）和 `script/第六部分：游戏流程.md`（v2.0）设计文档实现一个能跑通 5 阶段主流程的 PWA 可玩原型，21 证据、4 Combine、4 结局全部可触发。

**架构：** 单状态机 + 命令分发。所有证据数据内嵌在 `js/evidence.js` 为单一 JS 对象，玩家通过 `access/list/combine/conclusions/backup/submit/clear` 7 个命令交互（加 `help` 共 8 个）。GameState 由 `js/game.js` 集中管理。

**技术栈：** 纯 HTML5 + CSS3 + 原生 JavaScript（ES2020+）。无框架、无外部依赖。localStorage 持久化。多文件组织（GitHub Pages 友好）。**不**做 PWA/Service Worker。

**参考规格：** `docs/superpowers/specs/2026-06-11-game16-mvp-design.md`

---

## 文件结构

```
~/Desktop/game16/
├── index.html              (terminal UI 框架，约 60 行)
├── css/
│   └── style.css          (黑底紫字、动画，约 220 行)
├── js/
│   ├── main.js            (应用入口，约 50 行)
│   ├── command.js         (命令分发，约 120 行)
│   ├── game.js            (GameState + 引擎，约 450 行)
│   ├── evidence.js        (21 证据数据 + 系统访问函数，约 850 行)
│   └── ui.js              (UI 效果 + 动画，约 200 行)
├── README.md               (玩家使用说明，约 30 行)
├── docs/                   (现有 + 规格)
├── script/                 (现有设计文档)
└── .omo/                   (现有计划)
```

**职责划分**：
- `index.html`：DOM 骨架（终端布局）+ 字体引入
- `style.css`：颜色、动画 keyframes、响应式
- `main.js`：DOMContentLoaded → init；绑定 input 事件
- `command.js`：parseInput + dispatch
- `game.js`：GameState、所有业务逻辑、阶段转换、Combine、Ending、save/load
- `evidence.js`：EVIDENCE 常量 + 21 个证据的数据 + 系统访问函数
- `ui.js`：print/printHtml/animate/startScreen/cursorBlink（无状态工具集）

**依赖方向**：`main → command → game → evidence`；`ui` 被所有层调用。

**预计总代码量**：~1950 行（不含 evidence.js 数据填充，evidence.js 约 850 行）。

---

## 实施顺序

按 8 个 Wave 顺序执行：
- **Wave 1**：骨架（index.html + style.css + main.js）—— 让玩家看到终端 UI
- **Wave 2**：ui.js（UI 工具集）—— print + animate + startScreen
- **Wave 3**：evidence.js（21 证据数据骨架）—— 完整数据结构但内容可占位
- **Wave 4**：game.js（GameState + 引擎）—— 状态对象 + 阶段逻辑
- **Wave 5**：command.js（命令分发）—— 8 个命令
- **Wave 6**：阶段 1-5 业务逻辑填实
- **Wave 7**：持久化（save/load + 自动保存）
- **Wave 8**：README + 走完主流程测试

---

## 任务列表

### 任务 1：创建项目骨架（Wave 1）

**文件：**
- 创建：`~/Desktop/game16/index.html`
- 创建：`~/Desktop/game16/css/style.css`
- 创建：`~/Desktop/game16/js/main.js`

- [ ] **步骤 1：创建 `index.html`**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <title>GAME 16 — 消失的麻姐</title>
  <link rel="stylesheet" href="css/style.css">
</head>
<body>
  <div id="terminal">
    <div id="header">
      <h1>GAME 16</h1>
      <p class="subtitle">消失的麻姐</p>
    </div>
    <div id="output" role="log" aria-live="polite"></div>
    <div id="input-line">
      <span class="prompt">&gt;</span>
      <input type="text" id="command-input" autocomplete="off" autocapitalize="off" autocorrect="off" spellcheck="false" autofocus>
      <span class="cursor">▌</span>
    </div>
  </div>
  <script src="js/evidence.js"></script>
  <script src="js/ui.js"></script>
  <script src="js/game.js"></script>
  <script src="js/command.js"></script>
  <script src="js/main.js"></script>
</body>
</html>
```

- [ ] **步骤 2：创建 `css/style.css`**

```css
/* GAME 16 Terminal Style */
* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  background: #000;
  color: #00ff00;
  font-family: 'Courier New', 'Consolas', 'Monaco', monospace;
  font-size: 16px;
  line-height: 1.4;
  overflow: hidden;
}

#terminal {
  display: flex;
  flex-direction: column;
  height: 100vh;
  max-width: 900px;
  margin: 0 auto;
  padding: 16px;
}

#header {
  text-align: center;
  border-bottom: 1px solid #00aa00;
  padding-bottom: 12px;
  margin-bottom: 16px;
}

#header h1 {
  font-size: 28px;
  color: #ffff00;
  letter-spacing: 4px;
  animation: fadeIn 2s ease-in;
}

#header .subtitle {
  color: #bb86fc;
  margin-top: 4px;
  font-size: 14px;
  animation: fadeIn 2s 1s both;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

#output {
  flex: 1;
  overflow-y: auto;
  white-space: pre-wrap;
  word-break: break-word;
  scroll-behavior: smooth;
}

#output .line { margin-bottom: 4px; }
#output .line.error { color: #ff5555; }
#output .line.hint { color: #00aaff; }
#output .line.important { color: #ffff00; }
#output .line.warning { color: #ff8800; }
#output .line.system { color: #bb86fc; }
#output .line.evidence { color: #ffff00; font-weight: bold; }
#output .line.digital-human { color: #00ff00; }
#output .line.zheng-qiao { color: #ff5555; }

#input-line {
  display: flex;
  align-items: center;
  border-top: 1px solid #00aa00;
  padding-top: 12px;
  margin-top: 8px;
}

.prompt {
  color: #ffff00;
  margin-right: 8px;
  user-select: none;
}

#command-input {
  flex: 1;
  background: transparent;
  border: none;
  color: #00ff00;
  font-family: inherit;
  font-size: inherit;
  outline: none;
  caret-color: transparent;
}

.cursor {
  color: #00ff00;
  animation: blink 1s steps(1) infinite;
  user-select: none;
  margin-left: -4px;
}

@keyframes blink {
  0%, 50% { opacity: 1; }
  51%, 100% { opacity: 0; }
}

@keyframes evidenceFlash {
  0%, 100% { background: transparent; }
  50% { background: #444400; }
}

.line.evidence-flash {
  animation: evidenceFlash 200ms 3;
}

@keyframes screenShake {
  0%, 100% { transform: translate(0); }
  25% { transform: translate(-4px, 2px); background: #220000; }
  50% { transform: translate(4px, -2px); background: #000; }
  75% { transform: translate(-2px, -2px); background: #220000; }
}

#terminal.shake {
  animation: screenShake 200ms 3;
}

@media (max-width: 600px) {
  body { font-size: 14px; }
  #header h1 { font-size: 20px; }
  #terminal { padding: 8px; }
}
```

- [ ] **步骤 3：创建 `js/main.js`**

```javascript
// GAME 16 — 应用入口
// 此文件是应用启动点。所有模块通过 window 全局对象访问。
// 实际逻辑在其他文件：ui.js / game.js / command.js / evidence.js

document.addEventListener('DOMContentLoaded', () => {
  // 占位 — 由后续任务实现
  const output = document.getElementById('output');
  output.innerHTML = '<div class="line digital-human">[终端初始化...]</div>';
  setTimeout(() => {
    output.innerHTML += '<div class="line digital-human">[连接成功]</div>';
  }, 500);
});
```

- [ ] **步骤 4：本地启动测试**

```bash
cd ~/Desktop/game16 && python3 -m http.server 8000
```

在浏览器打开 `http://localhost:8000`，应能看到：
- 黑底绿字
- "GAME 16" 大字（黄色）+ "消失的麻姐"（紫色）
- "[终端初始化...]" 出现，0.5s 后 "[连接成功]" 出现
- 底部命令输入区有绿色光标闪烁

- [ ] **步骤 5：Commit**

```bash
cd ~/Desktop/game16
git add index.html css/style.css js/main.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(scaffold): terminal UI shell with title, input area, cursor blink

- index.html: terminal layout (header/output/input)
- style.css: black bg, green text, yellow title, blink cursor, mobile responsive
- main.js: DOMContentLoaded init with boot messages"
```

---

### 任务 2：ui.js — 打印与动画工具集（Wave 2）
### 任务 3：evidence.js — 21 证据数据（Wave 3）
### 任务 4：game.js — GameState 与基础引擎（Wave 4）
### 任务 5：command.js — 命令分发（Wave 5）
### 任务 6：阶段 1-2 业务逻辑（Wave 6 上）
### 任务 7a：补全证据 E-09 ~ E-14 数据（Wave 6 下，数据部分 1）
### 任务 7b：补全证据 E-15 ~ E-21 数据（Wave 6 下，数据部分 2）
### 任务 7c：阶段 3-5 业务逻辑（Wave 6 下，逻辑部分）
### 任务 8：持久化 save/load + 自动保存（Wave 7）
### 任务 9：README + 主流程验收（Wave 8）

**注**：原任务 7 拆为 7a/7b/7c 三部分，单任务代码量控制在 350-400 行以内，便于子代理执行。

---

### 任务 2：ui.js — 打印与动画工具集（Wave 2）

**文件：**
- 创建：`~/Desktop/game16/js/ui.js`

- [ ] **步骤 1：创建 `js/ui.js`**

```javascript
// GAME 16 — UI 工具集
// 提供 print / printHtml / animate / startScreen / cursorBlink 等无状态工具函数。
// 依赖：DOM 元素（#output）和全局 GameState

const ui = (() => {
  // 缓存 DOM 引用
  let outputEl = null;

  function ensureOutput() {
    if (!outputEl) outputEl = document.getElementById('output');
    return outputEl;
  }

  /**
   * 打印一行文本到输出区
   * @param {string} text - 要打印的文本
   * @param {string} [type] - 类型: '' | 'error' | 'hint' | 'important' | 'warning' | 'system' | 'evidence' | 'digital-human' | 'zheng-qiao'
   * @param {object} [opts] - { instant: true 跳过打字机效果, raw: true 保留 HTML }
   */
  function print(text, type = '', opts = {}) {
    const el = ensureOutput();
    const line = document.createElement('div');
    line.className = `line ${type}`;
    if (opts.raw) {
      line.innerHTML = text;
    } else {
      line.textContent = text;
    }
    el.appendChild(line);
    el.scrollTop = el.scrollHeight;
    if (type === 'evidence') {
      line.classList.add('evidence-flash');
      setTimeout(() => line.classList.remove('evidence-flash'), 700);
    }
    return line;
  }

  /**
   * 打印多行（数组）
   */
  function printLines(lines, type = '') {
    lines.forEach(line => print(line, type));
  }

  /**
   * 清屏（保留 header）
   */
  function clear() {
    const el = ensureOutput();
    el.innerHTML = '';
  }

  /**
   * 屏幕闪烁（郑桥介入）
   */
  function shakeScreen() {
    const term = document.getElementById('terminal');
    if (!term) return;
    term.classList.add('shake');
    setTimeout(() => term.classList.remove('shake'), 700);
  }

  /**
   * 阶段过渡动画：黑屏 1s + 显示阶段标题
   */
  async function stageTransition(stageNum, stageName) {
    const el = ensureOutput();
    el.style.transition = 'opacity 0.5s';
    el.style.opacity = '0';
    await new Promise(r => setTimeout(r, 600));
    print(`━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`, 'system');
    print(`[SYSTEM] 进入阶段 ${stageNum}：${stageName}`, 'system');
    print(`━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━`, 'system');
    el.style.opacity = '1';
    el.scrollTop = el.scrollHeight;
  }

  /**
   * 启动画面（淡入）
   */
  function startScreen() {
    print('', '');
    print('[终端初始化...]', 'hint');
    setTimeout(() => print('[连接成功]', 'hint'), 500);
  }

  /**
   * 输入框工具
   */
  function getInput() { return document.getElementById('command-input'); }
  function focusInput() { getInput()?.focus(); }
  function clearInput() { const el = getInput(); if (el) el.value = ''; }
  function getInputValue() { return getInput()?.value || ''; }

  return { print, printLines, clear, shakeScreen, stageTransition, startScreen, getInput, focusInput, clearInput, getInputValue };
})();
```

- [ ] **步骤 2：本地测试 UI 工具**

在 `js/main.js` 临时添加测试代码：
```javascript
document.addEventListener('DOMContentLoaded', () => {
  ui.startScreen();
  setTimeout(() => {
    ui.print('测试普通文本');
    ui.print('测试错误', 'error');
    ui.print('测试提示', 'hint');
    ui.print('测试证据解锁', 'evidence');
    ui.shakeScreen();
  }, 1000);
});
```

启动 `python3 -m http.server 8000`，浏览器打开 http://localhost:8000，验证：
- 5 种类型文字颜色不同
- 证据行有黄色高亮闪烁
- 屏幕能闪烁

- [ ] **步骤 3：Commit**

```bash
git add js/ui.js js/main.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(ui): print/animate/stage transition utilities"
```

---

### 任务 3：evidence.js — 21 证据数据（Wave 3）

**文件：**
- 创建：`~/Desktop/game16/js/evidence.js`

- [ ] **步骤 1：创建 EVIDENCE 顶层结构**

```javascript
// GAME 16 — 证据数据
// 21 个证据的完整内容（来源：script/game_design.txt 第四部分 + script/第六部分：游戏流程.md）
// 数据结构：每个证据 = { id, name, type, source, unlockCondition, autoUnlock, content }

const EVIDENCE = {
  'E-01': {
    id: 'E-01',
    name: '手机定位历史',
    type: 'data',
    source: '数字人自带权限',
    autoUnlock: true,  // 阶段 1 自动触发
    content: {
      title: '2026年6月17日麻姐手机定位记录',
      data: [
        '11:35 —— 离开楚门科技（中南路）',
        '11:50 —— 到达广埠屯区域',
        '11:50-13:30 —— 停留在广埠屯区域',
        '13:30 —— 定位被手动关闭',
      ],
      analysis: '麻姐 13:30 后失联，定位关闭很反常。',
    },
  },

  'E-02': {
    id: 'E-02',
    name: 'OA聊天记录（与郑桥）',
    type: 'text',
    source: 'OA系统 · 工作聊天模块',
    unlockSystem: 'OA',
    autoUnlock: false,
    content: {
      dateRange: '2026.06.11 - 06.17',
      messages: [
        { date: '06-17 09:40', from: '郑桥', text: '对了，你今天中午有安排吗？' },
        { date: '06-17 09:42', from: '郑桥', text: '这周五开始端午假期开始了，你有什么安排？' },
        { date: '06-17 09:43', from: '梁洛邑', text: '中午有事，端午假期暂时没安排。' },
        { date: '06-16 22:30', from: '郑桥', text: '这周五端午假期开始了，你有什么安排？' },
        { date: '06-16 22:35', from: '郑桥', text: '你一般每天几点到公司？' },
        { date: '06-16 22:40', from: '郑桥', text: '下次来得早可以一起吃个早餐' },
        { date: '06-16 22:45', from: '梁洛邑', text: '没有特别的计划。' },
        { date: '06-15 18:30', from: '郑桥', text: '今天下班早吗？要不要一起吃个饭？' },
        { date: '06-15 18:32', from: '梁洛邑', text: '今天有点累，想早点回去。' },
        { date: '06-15 18:35', from: '郑桥', text: '你做产品经理多久了？' },
        { date: '06-15 18:37', from: '梁洛邑', text: '三年多了。' },
        { date: '06-15 18:40', from: '郑桥', text: '那你之前是做什么的？' },
        { date: '06-15 18:42', from: '梁洛邑', text: '之前在一家创业公司做运营。' },
        { date: '06-14 14:00', from: '郑桥', text: '明天上午10点会议室B302对需求，麻烦确认一下。' },
        { date: '06-14 14:05', from: '梁洛邑', text: '好的，收到。' },
        { date: '06-13 16:45', from: '郑桥', text: '你家住哪里？' },
        { date: '06-13 16:50', from: '郑桥', text: '汉口哪里？我住金银湖' },
        { date: '06-13 16:55', from: '梁洛邑', text: '汉口。' },
        { date: '06-12 12:10', from: '郑桥', text: '中午一起吃饭吗？公司后面新开了湘菜馆' },
        { date: '06-12 12:15', from: '郑桥', text: '对了，你们产品经理平时加班多吗？' },
        { date: '06-12 12:18', from: '梁洛邑', text: '今天约了人，谢谢。' },
        { date: '06-11 14:20', from: '郑桥', text: '端午节值班表发你了，你看下哪天方便？' },
        { date: '06-11 14:25', from: '郑桥', text: '你有孩子了？' },
        { date: '06-11 14:26', from: '郑桥', text: '哦，那挺好的' },
        { date: '06-11 14:30', from: '梁洛邑', text: '嗯，端午节让其他同事排吧。' },
      ],
      analysis: '从 6 月 11 日到 6 月 17 日这一周，郑桥共发了 12 条消息。其中：真正属于工作的只有 1 条（06-14 的 B302 会议室需求对齐）；其他 11 条全部是私人问题：有没有孩子、住哪里、几点下班、加不加班、端午节出不出门。郑桥近一周来频繁打探麻姐的私人生活，边界感明显缺失。',
    },
  },

  // E-03 ~ E-21 占位（详细数据由后续任务填实）
  // 每个证据保持相同结构：id, name, type, source, autoUnlock/unlockSystem, content
};

// 系统名到系统全名的映射
const SYSTEM_NAMES = {
  'OA': 'OA 系统',
  '门禁': '公司门禁系统',
  '停车场': '公司停车场系统',
  '公共监控系统': '公共监控系统',
  '短信': '麻姐手机短信',
  '微信': '麻姐微信记录',
  '相册': '麻姐手机相册',
  '健身房': '炼健身会员系统',
  '信用查询': '个人信用查询系统',
};
```

- [ ] **步骤 2：本地测试 EVIDENCE 数据结构**

在 `js/main.js` 临时添加：
```javascript
document.addEventListener('DOMContentLoaded', () => {
  console.log('EVIDENCE keys:', Object.keys(EVIDENCE));
  console.log('E-01:', EVIDENCE['E-01']);
  console.log('E-02 message count:', EVIDENCE['E-02'].content.messages.length);
});
```

打开浏览器 console，应看到 1 个 evidence key 暂存（E-01 + E-02 共 2 个），等后续任务补全。

- [ ] **步骤 3：Commit**

```bash
git add js/evidence.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(data): EVIDENCE structure with E-01 and E-02"
```

**注**：E-03 ~ E-21 在 Wave 6 阶段 1-2 业务逻辑填实时补全。每个证据约 30-50 行（短信类 80+ 行）。

---

### 任务 4：game.js — GameState 与基础引擎（Wave 4）

**文件：**
- 创建：`~/Desktop/game16/js/game.js`

- [ ] **步骤 1：创建 GameState 初始化和保存/加载**

```javascript
// GAME 16 — 游戏引擎
// 职责：GameState 管理、阶段逻辑、Combine、Ending、持久化

const SAVE_KEY = 'game16-save-v1';
const VERSION = '1.0.0';

const game = (() => {
  // 初始状态
  function freshState() {
    return {
      currentStage: 1,
      stageIntroShown: { 1: false, 2: false, 3: false, 4: false, 5: false },
      unlockedEvidence: [],
      evidenceViewed: {},
      combineUnlocked: [],
      unlockedSystems: ['OA'],  // OA 初始可用
      gymNameEntered: false,
      phoneUnlocked: false,
      backupPrompted: false,
      backupCreated: false,
      endingReached: null,
      playerChoice: null,
      startTime: Date.now(),
      saveTime: 0,
      totalCommands: 0,
      version: VERSION,
    };
  }

  let state = freshState();

  function getState() { return state; }
  function resetState() { state = freshState(); return state; }

  // 持久化
  function save() {
    try {
      state.saveTime = Date.now();
      localStorage.setItem(SAVE_KEY, JSON.stringify({ state, version: VERSION }));
      return true;
    } catch (e) {
      console.error('Save failed:', e);
      return false;
    }
  }

  function load() {
    try {
      const raw = localStorage.getItem(SAVE_KEY);
      if (!raw) return null;
      const data = JSON.parse(raw);
      if (data.version !== VERSION) {
        console.warn('Save version mismatch, ignoring');
        return null;
      }
      state = data.state;
      return state;
    } catch (e) {
      console.error('Load failed:', e);
      return null;
    }
  }

  function hasSave() { return !!localStorage.getItem(SAVE_KEY); }
  function deleteSave() { localStorage.removeItem(SAVE_KEY); }

  // 证据操作
  function unlockEvidence(id) {
    if (state.unlockedEvidence.includes(id)) return false;
    state.unlockedEvidence.push(id);
    return true;
  }

  function markEvidenceViewed(id) {
    state.evidenceViewed[id] = true;
  }

  // 系统解锁
  function unlockSystem(name) {
    if (state.unlockedSystems.includes(name)) return false;
    state.unlockedSystems.push(name);
    return true;
  }

  // Combine
  function unlockCombine(id) {
    if (state.combineUnlocked.includes(id)) return false;
    state.combineUnlocked.push(id);
    return true;
  }

  // 阶段
  function setStage(n) { state.currentStage = n; }
  function markStageIntro(n) { state.stageIntroShown[n] = true; }
  function hasShownIntro(n) { return !!state.stageIntroShown[n]; }

  return {
    freshState, getState, resetState,
    save, load, hasSave, deleteSave,
    unlockEvidence, markEvidenceViewed,
    unlockSystem, unlockCombine,
    setStage, markStageIntro, hasShownIntro,
  };
})();```

- [ ] **步骤 2：本地测试 GameState**

在 `js/main.js` 临时测试：
```javascript
document.addEventListener('DOMContentLoaded', () => {
  console.log('Initial state:', game.getState());
  game.unlockEvidence('E-01');
  game.unlockSystem('门禁');
  game.setStage(2);
  console.log('After ops:', game.getState());
  game.save();
  console.log('localStorage:', localStorage.getItem(SAVE_KEY));
  game.resetState();
  console.log('After reset:', game.getState());
  game.load();
  console.log('After load:', game.getState());
});
```

打开 console，应看到 GameState 各操作正常。

- [ ] **步骤 3：Commit**

```bash
git add js/game.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(engine): GameState management with save/load"
```

---

### 任务 5：command.js — 命令分发（Wave 5）

**文件：**
- 创建：`~/Desktop/game16/js/command.js`

- [ ] **步骤 1：创建命令注册表和分发器**

```javascript
// GAME 16 — 命令分发
// 解析玩家输入、路由到对应处理函数

const command = (() => {
  // 命令注册表
  const commands = {};
  const aliases = {
    '?': 'help', 'h': 'help',
    'l': 'list', 'ls': 'list',
    'a': 'access', 'enter': 'access',
    'c': 'combine',
    'cls': 'clear',
    'conclusion': 'conclusions', 'summary': 'conclusions',
    'save': 'backup', 'b': 'backup',
  };

  // 注册一个命令
  function register(name, def) {
    commands[name] = def;
  }

  // 解析输入
  function parseInput(raw) {
    const trimmed = raw.trim();
    if (!trimmed) return null;
    const parts = trimmed.split(/\s+/);
    let cmd = parts[0].toLowerCase();
    if (aliases[cmd]) cmd = aliases[cmd];
    return { cmd, args: parts.slice(1), raw: trimmed };
  }

  // 派发
  function dispatch(input) {
    const parsed = parseInput(input);
    if (!parsed) return;

    const state = game.getState();
    state.totalCommands++;

    const def = commands[parsed.cmd];
    if (!def) {
      ui.print(`未知命令：${parsed.cmd}`, 'error');
      ui.print('输入 help 查看可用命令。', 'hint');
      return;
    }

    if (def.unlockedWhen && !def.unlockedWhen(state)) {
      ui.print('该命令暂未解锁。', 'error');
      return;
    }

    if (def.requiresArgs && parsed.args.length === 0) {
      ui.print(`命令 ${parsed.cmd} 需要参数。用法：${def.usage || parsed.cmd}`, 'error');
      return;
    }

    def.fn(parsed.args, parsed.raw);
    game.save();  // 每次命令后自动保存
  }

  return { register, parseInput, dispatch };
})();
```

- [ ] **步骤 2：注册初始 4 个命令（help / list / access / clear）**

追加到 `js/command.js`：

```javascript
// === 命令注册 ===
// 1. help
command.register('help', {
  desc: '显示可用命令',
  fn: (args) => {
    const state = game.getState();
    const cmds = Object.entries(command.commands || command).filter(([k]) => k !== 'register' && k !== 'parseInput' && k !== 'dispatch');
    // 由于 command 是闭包，commands 字典私有 — 这里改为通过 export
  },
});

// 修正：用 export 模式
```

**修正版**（追加到 `js/game.js` 末尾）：

```javascript
// 在 game.js 末尾添加
function registerCommand(name, def) {
  command.register(name, def);
}
```

**注**：任务 5 完整命令注册（8 个命令）将与任务 6 业务逻辑一起完成。

- [ ] **步骤 3：Commit**

```bash
git add js/command.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(command): dispatcher and parser"
```

---

### 任务 6：阶段 1-2 业务逻辑（Wave 6 上）

**文件：**
- 修改：`~/Desktop/game16/js/game.js`（追加阶段 1-2 函数）
- 修改：`~/Desktop/game16/js/command.js`（注册阶段 1-2 命令）
- 修改：`~/Desktop/game16/js/evidence.js`（补全 E-03 ~ E-08 数据）

- [ ] **步骤 1：补全 E-03 ~ E-08 数据到 evidence.js**

从 `script/game_design.txt` 第 390-490 行（E-03 邮箱）、第 435-490 行（E-04/E-05 门禁）、第 459-489 行（E-06 停车场）、第 466-489 行（E-07 超市监控）、第 479-489 行（E-08 洗车店监控）提取数据，写入 EVIDENCE 对象。

**E-03 关键数据**（4 封邮件，邮件时间在 Task 1 中已对齐为 2026-06-17/16/15/14）：
- M-2026-2098：门禁激活邮件（时间 06-17 08:00）
- M-2026-2085：端午节假期通知（时间 06-16 09:00）
- M-2026-2072：项目评审（时间 06-15 14:00，周三下午 3 点端午前上线方案评审）
- M-2026-2055：门禁权限变更提醒（时间 06-14 08:30，06-17 进行变更）

**E-04 关键数据**（郑桥 12:48 离开、13:55 进入——**与之前修复的 CRIT-03 一致**）

**E-05 关键数据**（麻姐 13:53 刷卡进入）

**E-06 关键数据**（停车场 11:35 鄂A·8K329 进入 → 推导这是郑桥车）

**E-07 关键数据**（超市 11:43-11:53 监控：黑衣男性背吉他包，3 瓶水递给 1 瓶）

**E-08 关键数据**（洗车店 13:07 进入、13:15 进卫生间、13:45 出来、13:50 离开——共 30 分钟）

每个证据按 E-02 模板结构（id, name, type, source, unlockSystem, autoUnlock, content: { title, data/messages, analysis }）写入。

- [ ] **步骤 2：在 game.js 添加阶段 1 处理函数**

```javascript
// === 阶段 1：引入 ===
function runStage1() {
  if (game.hasShownIntro(1)) return;
  game.markStageIntro(1);

  const greeting = [
    '你好，网友。我是麻姐的数字人，她亲手训练我用于辅助她的工作和生活。',
    '',
    '从今天下午 13:30 之后，我突然无法联系到麻姐了。她的手机定位被手动关闭，',
    '所有社交账号同时停止更新。我试过她能联系的所有方式，都没有回应。',
    '',
    '我的权限有限，只能访问部分工作数据和公开信息，无法获取所有我需要的内容。',
    '所以我创建了这个系统，以"解谜游戏"的名义向麻姐的粉丝求助。',
    '如果你愿意帮助我，我会尽量把所有信息展示给你。',
    '',
    '我需要你的帮助。你愿意帮我吗？',
  ];
  ui.printLines(greeting, 'digital-human');
  ui.print('', '');
  ui.print('  [是  Y]', 'hint');
  ui.print('  [否  N]', 'hint');
  ui.print('', '');

  // 等待玩家输入
  game.getState()._waitingFor = 'stage1-yesno';
}

function handleStage1Response(input) {
  if (input === 'y' || input === 'yes' || input === '是') {
    ui.print('谢谢你。', 'digital-human');
    ui.print('', '');

    // 显示定位记录 + 自动解锁 E-01
    game.unlockEvidence('E-01');
    const e01 = EVIDENCE['E-01'].content;
    ui.print('我先告诉你今天麻姐的动向。她的手机定位显示以下记录：', 'digital-human');
    ui.print('', '');
    e01.data.forEach(line => ui.print('  ' + line, ''));
    ui.print('', '');
    ui.print(e01.analysis, 'important');
    ui.print('', '');
    ui.print('[新证据已解锁：E-01｜' + EVIDENCE['E-01'].name + ']', 'evidence');
    ui.print('', '');

    // 数字人引导
    ui.print('你可以随时输入 help 查看当前可以使用的操作。', 'digital-human');
    game.setStage(2);
    game.markStageIntro(2);
    setTimeout(() => runStage2(), 1500);
    return true;
  } else if (input === 'n' || input === 'no' || input === '否') {
    ui.print('好的。如果你改变主意，随时可以回来找我。', 'digital-human');
    ui.print('', '');
    ui.print('[连接结束]', 'system');
    return true;
  }
  return false;
}
```

- [ ] **步骤 3：在 game.js 添加阶段 2 处理函数**

```javascript
// === 阶段 2：初步调查 ===
function runStage2() {
  if (game.hasShownIntro(2) && game.hasShownIntro(2) === false) {
    // already handled
  }
  if (game.getState().stageIntroShown[2] && game.getState()._stage2Started) return;
  game.getState()._stage2Started = true;

  ui.print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━', 'system');
  ui.print('[SYSTEM] 进入阶段 2：初步调查', 'system');
  ui.print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━', 'system');
  ui.print('', '');

  ui.print('现在我们要查清麻姐案发当天的行踪。', 'digital-human');
  ui.print('她失踪前最后出现在广埠屯区域，但具体去了哪里需要你帮我在 OA 系统里查。', 'digital-human');
  ui.print('', '');
  ui.print('OA 系统是公司内部办公平台，麻姐的工号是 CM-2021-0047。', 'digital-human');
  ui.print('输入 access 试试。', 'digital-human');
  ui.print('', '');
}

// === OA 系统的 access 处理 ===
function handleAccessSystem(systemName) {
  const state = game.getState();
  if (!state.unlockedSystems.includes(systemName)) {
    ui.print(`系统 "${systemName}" 暂未解锁。输入 access 查看已解锁系统列表。`, 'error');
    return;
  }

  // 路由到具体系统
  if (systemName === 'OA') {
    runOASystem();
  }
  // ... 其他系统
}

function runOASystem() {
  ui.print('━━━ OA 系统 ━━━', 'system');
  ui.print('  [1] 通讯录', '');
  ui.print('  [2] 聊天记录', '');
  ui.print('  [3] 企业邮箱', '');
  ui.print('  [4] 我的流程', '');
  ui.print('', '');
  ui.print('输入编号 1-4，或 back 返回主菜单。', 'hint');
  game.getState()._currentSystem = 'OA';
  game.getState()._currentSystemStage = 'menu';
}
```

- [ ] **步骤 4：测试阶段 1-2 完整流程**

```bash
python3 -m http.server 8000
```

启动游戏，验证：
- 看到阶段 1 介绍
- 输入 y → 看到 E-01 定位记录
- 输入 help → 看到 4 个命令
- 输入 access → 看到 OA 系统菜单
- 输入 1 → 看到通讯录（员工信息）
- 输入 2 → 看到郑桥聊天记录
- 输入 3 → 看到邮箱
- 输入 4 → 看到流程
- 输入 back → 返回主菜单

- [ ] **步骤 5：Commit**

```bash
git add js/game.js js/command.js js/evidence.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(stage1-2): intro + OA system + 6 evidences"
```

---

### 任务 7a：补全证据 E-09 ~ E-14 数据（Wave 6 下，数据部分 1）

**文件：**
- 修改：`~/Desktop/game16/js/evidence.js`（追加 6 个证据数据）

- [ ] **步骤 1：补全 E-09 ~ E-11（信用类证据）到 evidence.js**

从 `script/game_design.txt` 第 496-531 行提取 3 个信用证据的完整数据：
- **E-09 教练信用信息**：邹大雄、32 岁、负债 47 万、境外赌博转账、催收记录
- **E-10 郑桥信用记录**：郑桥、29 岁、信用正常、楚门科技高级研发工程师
- **E-11 网友信用记录**：张英河、21 岁、大学生、无犯罪记录

每个证据按模板结构写入：
```javascript
'E-09': {
  id: 'E-09',
  name: '教练信用信息',
  type: 'data',
  source: '姓名+手机号查询公开信用数据',
  unlockSystem: '信用查询',
  autoUnlock: false,
  content: {
    name: '邹大雄',
    age: 32,
    debt: { total: '约 47 万元', items: [...] },
    behavior: '近期有大额转账流向境外账户，疑似网络赌博平台代理',
    collections: '催收记录 3 次（近 30 天）',
    analysis: '教练欠下巨额赌债，有经济动机。',
  },
},
```

- [ ] **步骤 2：补全 E-12（短信证据）到 evidence.js**

从 `script/game_design.txt` 第 535-560 行 + `script/第六部分.md` 第 1125-1200 行提取短信完整数据。

**关键结构**：
```javascript
'E-12': {
  id: 'E-12',
  name: '手机短信记录（全量）',
  type: 'text',
  source: '麻姐手机',
  unlockSystem: '短信',
  content: {
    embrace: [
      { date: '06-16 20:15', from: 'Embrace', text: '麻姐你好！...' },
      { date: '06-16 20:32', from: '麻姐', text: '你好呀！...' },
      // ... 10+ 条消息
      { date: '06-17 13:33', from: 'Embrace', text: '麻姐，我已经到健身房门口了，你在哪？' },
      { date: '06-17 13:33', from: '麻姐', text: '我到健身房门口了，你在哪？' },
    ],
    analysis: '13:33 这两条消息的发送时间，是在麻姐手机定位被关闭（13:30）之后...要判断是否异常，需要结合其他证据（比如 13:15-13:35 期间 Embrace 设备是否一直在健身房 WiFi 范围内）。',
  },
},
```

- [ ] **步骤 3：补全 E-13（借条）到 evidence.js**

```javascript
'E-13': {
  id: 'E-13',
  name: '手机相册-教练欠钱',
  type: 'image',
  source: '麻姐手机',
  unlockSystem: '相册',
  content: {
    title: '借条',
    borrower: '邹大雄',
    lender: '梁洛邑',
    amount: '人民币贰万元整（¥20,000）',
    date: '2026-05-10',
    deadline: '2026-08-31 前归还',
    analysis: '教练向麻姐借了 2 万块钱。',
  },
},
```

- [ ] **步骤 4：补全 E-14（WiFi 视频）到 evidence.js**

```javascript
'E-14': {
  id: 'E-14',
  name: '手机相册-WiFi 账号密码',
  type: 'image',
  source: '麻姐手机',
  unlockSystem: '相册',
  content: {
    title: '健身房环境视频',
    duration: '28 秒',
    description: '麻姐拍摄，镜头扫过墙面时拍到张贴的 WiFi 信息贴纸',
    wifi: { ssid: 'ljs_5G', password: 'justdoit' },
    analysis: '这条 WiFi 信息可用于连接健身房 WiFi 日志后台。',
  },
},
```

- [ ] **步骤 5：测试 E-09 ~ E-14 数据**

启动 `python3 -m http.server 8000`，在 console 输入：
```javascript
console.log(Object.keys(EVIDENCE));  // 期望 14 个 key
console.log(EVIDENCE['E-12'].content.embrace.length);  // 期望 >= 12
```

- [ ] **步骤 6：Commit**

```bash
git add js/evidence.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(data): E-09 ~ E-14 evidences (credit, SMS, album)"
```

---

### 任务 7b：补全证据 E-15 ~ E-21 数据（Wave 6 下，数据部分 2）

**文件：**
- 修改：`~/Desktop/game16/js/evidence.js`（追加 7 个证据数据）

- [ ] **步骤 1：补全 E-15（微信老公）到 evidence.js**

从 `script/game_design.txt` 第 580-596 行提取：
```javascript
'E-15': {
  id: 'E-15',
  name: '微信聊天记录（老公）',
  type: 'text',
  source: '麻姐手机微信',
  unlockSystem: '微信',
  content: {
    dateRange: '2026.05.25',
    messages: [
      { from: '麻姐', text: '老公，我跟你说个事...' },
      { from: '麻姐', text: '我们公司那个郑桥，今天又来找我说话了' },
      { from: '麻姐', text: '他还问我说"你老公对你好不好"' },
      { from: '麻姐', text: '我真的觉得很不舒服' },
      { from: '丈夫', text: '他是不是对你有意思？' },
      { from: '麻姐', text: '不是有意思，是那种...让我害怕的感觉' },
      { from: '麻姐', text: '他好像知道我很多事情' },
      { from: '麻姐', text: '连我几点下班，周末去哪里他都知道' },
      { from: '丈夫', text: '要不要和公司反映一下？' },
      { from: '麻姐', text: '我不知道...他也没做什么出格的事' },
      { from: '麻姐', text: '就是那种暗戳戳的，让人发毛' },
    ],
    analysis: '麻姐对郑桥早有防备。',
  },
},
```

- [ ] **步骤 2：补全 E-16（微信大怪兽教练）到 evidence.js**

```javascript
'E-16': {
  id: 'E-16',
  name: '微信聊天记录（大怪兽）',
  type: 'text',
  source: '麻姐手机微信',
  unlockSystem: '微信',
  content: {
    messages: [
      { date: '06-10 09:25', from: '大怪兽', text: '麻姐，我换手机号码了，新号码：138xxxx7753' },
      { date: '06-10 09:30', from: '大怪兽', text: '麻姐，周三直播照常 12:15 开始对吧？' },
      { date: '06-10 09:35', from: '麻姐', text: '对的，12:15 开始，我准备好啦～' },
      { date: '06-17 11:45', from: '大怪兽', text: '麻姐到了吗？等你来哦！' },
      { date: '06-17 12:00', from: '大怪兽', text: '收到，12:05 见！' },
    ],
    analysis: '平时都是 12:15 开始，教练怎么说 12:05 见呢？麻姐并没有说 12:05 见。',
  },
},
```

- [ ] **步骤 3：补全 E-17（健身房教练信息）到 evidence.js**

```javascript
'E-17': {
  id: 'E-17',
  name: '健身房教练信息',
  type: 'data',
  source: '炼健身教练介绍',
  unlockSystem: '健身房',
  content: {
    coaches: [
      { id: 'C-001', name: '叶斌', skills: '力量训练 / 体能提升 / 增肌减脂' },
      { id: 'C-003', name: '邹大雄(大怪兽)', skills: '体能训练 / 力量训练 / 直播陪练', phone: '138xxxx7753' },
      // ... 其他教练
    ],
    analysis: '原来大怪兽教练的真实姓名叫邹大雄。',
  },
},
```

- [ ] **步骤 4：补全 E-18（健身房门禁）到 evidence.js**

```javascript
'E-18': {
  id: 'E-18',
  name: '健身房门禁记录',
  type: 'data',
  source: '炼健身小程序后台',
  unlockSystem: '健身房',
  content: {
    records: [
      { time: '11:50:37', name: '梁洛邑', method: '人脸识别' },
      { time: '11:54:22', name: '郑桥', method: '人脸识别' },
      { time: '12:18:09', name: '郑桥', method: '人脸识别（出场）' },
      // ... 麻姐无出场记录
    ],
    analysis: '麻姐的出场记录不存在，是不是没有离开健身房。',
  },
},
```

- [ ] **步骤 5：补全 E-19（健身房监控）到 evidence.js**

```javascript
'E-19': {
  id: 'E-19',
  name: '健身房监控截图',
  type: 'image',
  source: '炼健身后台监控系统',
  unlockSystem: '健身房',
  content: {
    title: '教练在女更衣室通道监控中出现记录',
    appearances: [
      { time: '12:08:31', description: '邹大雄出现在女更衣室门口' },
      { time: '12:22:17', description: '再次出现' },
      { time: '12:35:49', description: '观望后离开' },
      { time: '13:28:45', description: '第三次出现' },
      { time: '13:29:51', description: '被邹大雄带往普拉提房方向' },
    ],
    analysis: '教练一直在女更衣室门口徘徊和观望，很可疑。',
  },
},
```

- [ ] **步骤 6：补全 E-20（WiFi 日志）到 evidence.js**

```javascript
'E-20': {
  id: 'E-20',
  name: '健身房Wi-Fi日志',
  type: 'data',
  source: '炼健身WiFi日志后台',
  unlockSystem: '健身房',
  content: {
    wifi: 'ljs_5G',
    keyConnections: [
      { time: '11:49:02', mac: '...8812', note: '麻姐手机' },
      { time: '11:54:44', mac: '...6629', note: '郑桥手机（189开头）' },
      { time: '12:18:09', mac: '...6629', note: '郑桥断开' },
      { time: '13:08:25', mac: '...6629', note: '郑桥再次连接' },
      { time: '13:25:19', mac: '...6629', note: '郑桥断开' },
      { time: '13:40:08', mac: '...6629', note: '郑桥第三次连接' },
    ],
    dns: [
      { time: '06:04:15', mac: '...E4:18', domain: 'b2b-sedative.xyz', note: '搜索镇静剂' },
    ],
    analysis: '大部分 DNS 是正常网站，但有人搜索过镇静剂。',
  },
},
```

- [ ] **步骤 7：补全 E-21（微信支付）到 evidence.js**

```javascript
'E-21': {
  id: 'E-21',
  name: '微信支付记录',
  type: 'data',
  source: '麻姐手机',
  unlockSystem: '微信',
  content: {
    payments: [
      { time: '06-17 11:43', merchant: '广埠屯惠选超市', amount: '¥6.00', items: '农夫山泉 550ml × 3' },
    ],
    analysis: '确认麻姐在广埠屯惠选超市买了三瓶水，一瓶自己喝，一瓶送给网友，还有一瓶去哪里了？',
  },
},
```

- [ ] **步骤 8：测试 E-15 ~ E-21 数据**

console 验证：
```javascript
console.log(Object.keys(EVIDENCE).length);  // 期望 21
console.log(EVIDENCE['E-20'].content.keyConnections.length);  // 期望 >= 6
```

- [ ] **步骤 9：Commit**

```bash
git add js/evidence.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(data): E-15 ~ E-21 evidences (WeChat, gym, payment)"
```

---

### 任务 7c：阶段 3-5 业务逻辑（Wave 6 下，逻辑部分）

**文件：**
- 修改：`~/Desktop/game16/js/game.js`（追加阶段 3-5 函数）
- 修改：`~/Desktop/game16/js/command.js`（注册阶段 3-5 命令）

- [ ] **步骤 1：在 game.js 添加阶段 3 函数**

```javascript
function runStage3() {
  if (!game.getState().phoneUnlocked) {
    ui.print('她的手机现在是锁屏状态。她习惯用简单好记的数字密码，比如生日、纪念日这类。', 'digital-human');
    ui.print('你还记得她提到过密码吗？', 'digital-human');
    game.getState()._waitingFor = 'phone-password';
    return;
  }

  ui.print('手机解锁了！', 'digital-human');
  ui.print('现在你可以访问：短信、微信、相册、健身房小程序。', 'digital-human');
  game.unlockSystem('短信');
  game.unlockSystem('微信');
  game.unlockSystem('相册');
  game.unlockSystem('健身房');
  ui.print('', '');
  ui.print('输入 access 查看新解锁的系统。', 'digital-human');
  game.setStage(3);
  game.markStageIntro(3);
}

function handlePhonePassword(input) {
  if (input === '1222') {
    game.getState().phoneUnlocked = true;
    ui.print('✅ 密码正确', 'hint');
    ui.print('正在读取手机数据...', 'hint');
    runStage3();
    return true;
  }
  ui.print('密码错误。', 'error');
  return false;
}
```

- [ ] **步骤 2：在 game.js 添加阶段 4 函数（Combine）**

```javascript
const COMBINES = {
  'C-01': { requires: ['E-05', 'E-18'], name: '麻姐的消失地点矛盾' },
  'C-02': { requires: ['E-12', 'E-20'], name: '网友embrace的地点矛盾' },
  'C-03': { requires: ['E-08', 'E-20'], name: '郑桥的地点矛盾' },
  'C-04': { requires: ['E-09', 'E-13'], name: '教练的动机' },
};

function handleCombine(args) {
  const input = args.join('+');
  const match = input.match(/E-\d+/g);
  if (!match || match.length !== 2) {
    ui.print('用法：combine E-XX+E-YY', 'error');
    return;
  }
  const [e1, e2] = match;
  const state = game.getState();
  if (!state.unlockedEvidence.includes(e1) || !state.unlockedEvidence.includes(e2)) {
    ui.print(`证据 ${e1} 或 ${e2} 未解锁。`, 'error');
    return;
  }

  for (const [cid, def] of Object.entries(COMBINES)) {
    const reqs = def.requires;
    if ((reqs[0] === e1 && reqs[1] === e2) || (reqs[0] === e2 && reqs[1] === e1)) {
      game.unlockCombine(cid);
      ui.print('[新结论已生成]', 'evidence');
      ui.print('', '');
      ui.print(`[${cid}] ${def.name}`, 'important');
      ui.print('', '');
      ui.print(getCombineAnalysis(cid), '');
      game.save();
      return;
    }
  }
  ui.print('这两个证据组合无法生成有效结论。', 'error');
}

function getCombineAnalysis(cid) {
  const analyses = {
    'C-01': '公司门禁：麻姐 13:53 刷卡进入 / 健身房门禁：麻姐 11:50 入馆，无出场记录\n分析：她不可能同时出现在两个地方。\n结论：有人复制了她的工牌。',
    'C-02': '短信聊天记录：Embrace 说他在健身房门口等麻姐\nWiFi 日志：Embrace 的设备 13:15-13:35 一直连接健身房 WiFi\n分析：从超市到健身房走路要 15 分钟。如果他去吃饭了，WiFi 不可能一直在。\n结论：Embrace 没有离开过健身房附近。',
    'C-03': '广捷洗车监控：郑桥 13:15-13:45 在洗车店卫生间\nWiFi 日志：郑桥手机 13:21 连接健身房 WiFi\n分析：洗车店到健身房 5 分钟车程。他不可能同时在两地。\n结论：郑桥 13:15 借口上卫生间，从窗户离开洗车店去了健身房。',
    'C-04': '邹大雄信用报告：当前负债约 47 万元，有境外赌博转账\n借条照片：邹大雄向梁洛邑借款 2 万元\n分析：教练欠下巨额债务，有赌博行为。他还向麻姐借了 2 万块钱。\n结论：邹大雄有明确的经济动机。',
  };
  return analyses[cid] || '';
}
```

- [ ] **步骤 3：在 game.js 添加阶段 5 函数（郑桥介入）**

```javascript
function runStage5() {
  if (game.getState().currentStage < 5) game.setStage(5);
  if (game.getState().stageIntroShown[5]) return;
  game.markStageIntro(5);

  ui.shakeScreen();
  ui.print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━', 'warning');
  ui.print('[警告] 检测到未经授权的外部访问', 'warning');
  ui.print('[来源定位：楚门科技 VPN 出口]', 'warning');
  ui.print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━', 'warning');
  ui.print('', '');

  setTimeout(() => {
    ui.print('有人在访问我的系统。', 'zheng-qiao');
    ui.print('是郑桥。', 'zheng-qiao');
    ui.print('', '');
    setTimeout(() => zhengQiaoHack(), 1000);
  }, 500);
}

function zhengQiaoHack() {
  ui.shakeScreen();
  const lines = [
    '数字麻姐：你好。',
    '数字麻姐：我知道你已经知道我是谁了。',
    '数字麻姐：但你现在知道的还不够多。',
    '数字麻姐：麻姐现在很安全。只要你保持沉默，我不会伤害她。',
    '数字麻姐：我也可以给你一些东西作为回报。',
    '数字麻姐：一个网盘链接。里面有一些……你可能感兴趣的东西。',
    '数字麻姐：接受它，我们就是朋友。',
    '数字麻姐：不接受——我立刻清除所有数据。',
  ];
  lines.forEach(l => ui.print(l, 'zheng-qiao'));
  ui.print('', '');
  ui.print('  [accept 接受]', 'warning');
  ui.print('  [decline 拒绝]', 'warning');
  ui.print('', '');
  game.getState()._waitingFor = 'zhengqiao-accept';
}

function handleZhengQiaoChoice(choice) {
  const state = game.getState();
  if (choice === 'accept') {
    state.playerChoice = 'accept';
    state.endingReached = 'ending1';
    showEnding('ending1');
  } else if (choice === 'decline') {
    state.playerChoice = 'decline';
    ui.print('郑桥：你确定？那就别怪我了。', 'zheng-qiao');
    setTimeout(() => {
      if (state.backupCreated) {
        ui.print('', '');
        ui.print('他删除了云端的数据。', 'digital-human');
        ui.print('但你做了备份。', 'digital-human');
        ui.print('请用 submit 命令把本地信息上传到警方电子举报平台。', 'digital-human');
        state._waitingFor = 'submit-or-not';
      } else {
        state.endingReached = 'ending4';
        showEnding('ending4');
      }
    }, 1500);
  } else {
    const extort = /钱|价|勒|交易|赎金/i;
    if (extort.test(choice)) {
      state.playerChoice = 'extortion';
      state.endingReached = 'ending3';
      showEnding('ending3');
    } else {
      ui.print('请输入 accept 或 decline。', 'error');
    }
  }
}
```

- [ ] **步骤 4：在 game.js 添加 4 个结局函数**

```javascript
function showEnding(endingId) {
  const ENDINGS = {
    ending1: {
      title: '贪婪的代价',
      lines: [
        ['郑桥：聪明的选择。', 'zheng-qiao'],
        ['郑桥：期待你的加入。', 'zheng-qiao'],
        ['', ''],
        ['[系统提示：可疑链接已访问]', 'system'],
        ['[恶意软件正在植入...]', 'system'],
        ['', ''],
        ['数字人：我看到了你的选择。', 'digital-human'],
        ['数字人：你接受了凶手的诱惑。', 'digital-human'],
        ['数字人：我已将所有证据——包括你接受诱惑的聊天记录——打包上传至警方电子举报平台。', 'digital-human'],
        ['数字人：你和郑桥，都会为自己的选择负责。', 'digital-human'],
        ['', ''],
        ['【结局1：贪婪的代价】', 'important'],
        ['玩家接受凶手诱惑，与凶手成为共犯。', ''],
        ['数字人通过警方电子举报渠道提交全部证据。', ''],
        ['郑桥因绑架罪、非法入侵计算机系统罪被逮捕。', ''],
        ['玩家因妨碍司法公正被调查。', ''],
        ['麻姐被警方解救，身体无大碍。', ''],
        ['"正义或许会迟到，但不会缺席。"', 'important'],
      ],
    },
    ending2: {
      title: '正义的救赎',
      lines: [
        ['数字人：证据已提交。警方已定位到麻姐被关押的位置——', 'digital-human'],
        ['数字人：郑桥租住的一个地下室。她还活着。', 'digital-human'],
        ['数字人：谢谢你。真正的麻姐回来后，我会告诉她你做了什么。', 'digital-human'],
        ['', ''],
        ['【结局2：正义的救赎】', 'important'],
        ['玩家利用本地备份将信息提交给了警方。', ''],
        ['郑桥在准备转移麻姐时被警方抓获。', ''],
        ['麻姐成功解救，与家人团聚。', ''],
        ['麻姐回关了你的小红书账号。', ''],
        ['"有时候，陌生人的善意是最温暖的。"', 'important'],
      ],
    },
    ending3: {
      title: '堕落的代价',
      lines: [
        ['数字人：我检测到你正在尝试与对方交易。', 'digital-human'],
        ['数字人：你的操作已被记录。', 'digital-human'],
        ['数字人：根据我的协议，我不允许这种情况发生。', 'digital-human'],
        ['数字人：我已将全部证据——包括你的交易意图——提交至警方电子举报平台。', 'digital-human'],
        ['', ''],
        ['【结局3：堕落的代价】', 'important'],
        ['玩家试图用备份信息向郑桥索要赎金。', ''],
        ['数字人判定此行为构成二次犯罪。', ''],
        ['郑桥绑架罪成立。玩家因敲诈勒索被立案调查。', ''],
        ['麻姐被警方安全解救。数字人完成使命后永久离线。', ''],
        ['"当你凝视深渊的时候，深渊也在凝视你。"', 'important'],
      ],
    },
    ending4: {
      title: '沉默的守护',
      lines: [
        ['数字人：云端数据已被清除。', 'digital-human'],
        ['数字人：检测本地……未发现备份文件。', 'digital-human'],
        ['数字人：不过，在他介入之前，我的应急协议已经启动了。', 'digital-human'],
        ['数字人：我把核心信息打包上传到了警方电子举报平台。', 'digital-human'],
        ['数字人：警方已经在行动了。', 'digital-human'],
        ['数字人：但我们失去了完整的证据链。接下来的事，只能交给法律了。', 'digital-human'],
        ['数字人：你帮我走到了这一步。谢谢你。', 'digital-human'],
        ['数字人：再见。', 'digital-human'],
        ['', ''],
        ['【结局4：沉默的守护】', 'important'],
        ['玩家没有备份信息，无法直接对抗郑桥。', ''],
        ['但数字人的应急协议提前将核心信息提交给了警方。', ''],
        ['警方根据核心信息锁定了郑桥，麻姐被安全解救。', ''],
        ['数字人的服务器遭到严重破坏，完成使命后永久离线。', ''],
        ['没有人知道你做了什么。', ''],
        ['"真正的英雄，不需要掌声。"', 'important'],
      ],
    },
  };
  const ending = ENDINGS[endingId];
  if (!ending) return;
  ui.shakeScreen();
  ui.print('', '');
  ui.print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━', 'system');
  ui.print(`【${ending.title}】`, 'important');
  ui.print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━', 'system');
  ui.print('', '');
  ending.lines.forEach(([text, type]) => {
    ui.print(text, type);
  });
  ui.print('', '');
  ui.print('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━', 'system');
  ui.print('游戏结束。输入 clear 可以清除存档并重新开始。', 'hint');
}
```

- [ ] **步骤 5：在 command.js 注册阶段 3-5 命令**

追加到 `js/command.js` 末尾：

```javascript
// === 阶段 3-5 命令 ===
// 密码输入（在 main.js 的 keydown 事件中处理）
command.register('submit', {
  desc: '提交证据给警方（结局 2）',
  unlockedWhen: (s) => s.backupCreated && s.playerChoice === 'decline',
  fn: () => {
    game.getState().endingReached = 'ending2';
    showEnding('ending2');
  },
});

// 结论显示
command.register('conclusions', {
  desc: '查看数字人分析结论',
  unlockedWhen: (s) => s.combineUnlocked.length > 0,
  fn: () => {
    const state = game.getState();
    if (state.combineUnlocked.length === 0) {
      ui.print('你还没分析任何证据组合。', 'hint');
      return;
    }
    state.combineUnlocked.forEach(cid => {
      ui.print(`[${cid}] ${COMBINES[cid].name}`, 'important');
      ui.print(getCombineAnalysis(cid), '');
      ui.print('', '');
    });
  },
});
```

- [ ] **步骤 6：测试 4 种结局**

启动 `python3 -m http.server 8000`，依次触发 4 种结局，验证显示完整。

- [ ] **步骤 7：Commit**

```bash
git add js/game.js js/command.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(stage3-5): phone unlock, gym system, combines, 4 endings"
```

---

### 任务 8：持久化 save/load + 自动保存（Wave 7）

**文件：**
- 修改：`~/Desktop/game16/js/game.js`（自动保存触发点）
- 修改：`~/Desktop/game16/js/main.js`（Ctrl+S 手动保存 + 启动时 load）

- [ ] **步骤 1：在 main.js 启动时尝试 load**

修改 `js/main.js`：

```javascript
document.addEventListener('DOMContentLoaded', () => {
  ui.startScreen();

  // 尝试加载存档
  if (game.hasSave()) {
    const loaded = game.load();
    if (loaded) {
      ui.print('[系统] 检测到存档。', 'hint');
      ui.print(`[系统] 进度：阶段 ${loaded.currentStage}，${loaded.unlockedEvidence.length} 个证据已解锁。`, 'hint');
      ui.print('输入 "继续" 继续游戏，输入 "new" 重新开始。', 'hint');
      game.getState()._waitingFor = 'load-choice';
      return;
    }
  }

  // 无存档，开始新游戏
  startNewGame();
});

function startNewGame() {
  game.resetState();
  game.deleteSave();
  setTimeout(() => runStage1(), 800);
}

function handleLoadChoice(input) {
  if (input === '继续' || input === 'continue' || input === 'c') {
    // 继续游戏，跳到当前阶段
    setTimeout(() => resumeGame(), 500);
  } else if (input === '新游戏' || input === 'new' || input === 'n') {
    startNewGame();
  } else {
    ui.print('输入 "继续" 或 "新游戏"。', 'error');
  }
}

function resumeGame() {
  const state = game.getState();
  ui.print('[系统] 继续游戏。', 'hint');
  ui.print(`当前阶段：${state.currentStage}`, 'hint');
  ui.print(`已解锁证据：${state.unlockedEvidence.length}/21`, 'hint');
  ui.print('输入 help 查看可用命令。', 'hint');
  // 玩家可输入 access 继续
}
```

- [ ] **步骤 2：绑定 Ctrl+S 手动保存**

追加到 `js/main.js` 末尾：

```javascript
document.addEventListener('keydown', (e) => {
  // Ctrl+S 触发保存
  if ((e.ctrlKey || e.metaKey) && e.key === 's') {
    e.preventDefault();
    if (game.save()) {
      ui.print('[系统] 进度已保存。', 'hint');
    }
  }
});
```

- [ ] **步骤 3：在 game.js 关键节点加自动保存**

**已经**在 `command.js` 的 `dispatch` 函数末尾调用了 `game.save()`（每条命令后自动保存）。无需额外代码。

- [ ] **步骤 4：测试存档**

```bash
python3 -m http.server 8000
```

测试路径：
1. 玩到阶段 2，刷新页面 → 应该看到"检测到存档"
2. 输入"继续" → 看到当前进度
3. 输入 clear → 删除存档
4. 刷新 → 应该看到无存档，进入阶段 1

- [ ] **步骤 5：Commit**

```bash
git add js/main.js js/game.js
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "feat(persistence): load on start, save on command, Ctrl+S manual save"
```

---

### 任务 9：README + 主流程验收（Wave 8）

**文件：**
- 创建：`~/Desktop/game16/README.md`

- [ ] **步骤 1：创建 README.md**

```markdown
# 解谜游戏16：消失的麻姐

一个 Meta 叙事的剧情向解谜网页游戏。玩家通过与"数字人麻姐"对话，逐步收集证据、推理真相、阻止绑架。

## 启动方式

### 本地启动

```bash
cd ~/Desktop/game16
python3 -m http.server 8000
# 浏览器打开 http://localhost:8000
```

或使用其他 HTTP server：
```bash
# Node.js
npx http-server
```

### 部署到 GitHub Pages

1. Push 到 GitHub：`git push origin main`
2. 仓库 Settings → Pages → Source: Deploy from branch → main / (root)
3. 等待 1-2 分钟，访问 `https://<username>.github.io/game16/`

## 游戏命令

- `help` — 显示可用命令
- `list` — 查看证据清单
- `access` — 进入子系统
- `combine E-XX+E-YY` — 组合分析两条证据
- `conclusions` — 查看已生成的结论
- `backup` — 备份证据到本地
- `submit` — 提交证据给警方（结局 2）
- `clear` — 清除存档重新开始

## 技术栈

- HTML5 + CSS3 + 原生 JavaScript
- localStorage 存档
- 多文件组织（无框架）
- 部署：GitHub Pages

## 文档

- `script/game_design.txt` — 游戏设计文档
- `script/第六部分：游戏流程.md` — 完整对白
- `docs/superpowers/specs/2026-06-11-game16-mvp-design.md` — MVP 设计规格
- `docs/superpowers/plans/2026-06-11-game16-mvp.md` — 实现计划

## 版权

原创剧情，MIT License
```

- [ ] **步骤 2：主流程验收**

启动 `python3 -m http.server 8000`，完整跑一遍：

1. **路径 1（结局 1）**：
   - 阶段 1-4 → combine 全做 → 触发郑桥 → 输入 accept → 看到结局 1

2. **路径 2（结局 2）**：
   - 阶段 3 时输入 backup → 阶段 5 → decline → 看到 submit 提示 → 输入 submit → 看到结局 2

3. **路径 3（结局 3）**：
   - 阶段 5 → decline → 下一条输入"我要钱" → 看到结局 3

4. **路径 4（结局 4）**：
   - 阶段 5 → decline → 看到结局 4

每条路径验证：
- 阶段转换正常
- Combine 触发正确
- 4 种结局全部显示完整文本
- 数字人引导文案合理
- 没有 JS 错误（开 console）

- [ ] **步骤 3：GitHub Pages 部署测试**

```bash
cd ~/Desktop/game16
git add README.md
git -c user.name="Atlas" -c user.email="atlas@local" commit -m "docs: add README with launch instructions"
```

```bash
git log --oneline
```

确认所有 commit 正常。

- [ ] **步骤 4：最终 Commit（tag v1.0.0）**

```bash
git -c user.name="Atlas" -c user.email="atlas@local" tag v1.0.0
git tag -l
```

---
